import { MessagesView } from "@/domain/messages";
import { NextPage } from "next";

const Main: NextPage = () => {
  return (
    <div>
      <MessagesView />
    </div>
  );
};

export default Main;
